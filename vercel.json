{
    "version": 2,
    "builds": [
      {
        "src": "bbs/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3" }
      },
      {
        "src": "build_files.sh",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "staticfiles"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "bbs/wsgi.py"
      }
    ],
    "outputDirectory": "staticfiles"
  }